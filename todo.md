# 頁內浮動播放器

## 目標

當動畫瘋影片頁面的原播放器可見高度低於 50%，且影片正在播放時，將完整播放器固定在頁面右下角。當原播放器位置恢復至至少 60% 可見時，退出浮動模式並回到原位。

這是頁面內的浮動播放器，不使用瀏覽器 Picture-in-Picture API，因此不受 PIP 使用者手勢與權限限制；切換分頁或切到其他應用程式後，不會像原生 PIP 一樣顯示在最上層。

## 行為

- 僅套用於動畫瘋影片頁與動畫派對頁。
- 只有影片正在播放時才自動進入浮動模式。
- 原播放器可見高度 `< 50%` 時進入浮動模式。
- 原播放器位置可見高度 `>= 60%` 時退出浮動模式。
- 50%／60% 之間保留目前狀態，避免捲動邊界抖動。
- 已經浮動後，即使影片暫停或播放結束也維持在右下角，直到原位置恢復至少 60% 可見。
- 進入全螢幕時退出浮動模式。
- 浮動播放器固定在右下角、使用高 z-index，並保留 Video.js 控制列與彈幕層。
- 浮動播放器四角保留透明縮放熱區；平常不顯示，滑鼠移入播放器時才顯示半透明 L 型角標。
- 使用者手動縮放後，同一頁面生命週期內再次浮動會沿用最後尺寸；reload 後回到預設大小。
- 除既有按鈕、連結、輸入框、Video.js 控制列與縮放點外，整個浮動 `.videoframe` 都可拖曳，並限制在瀏覽器可視範圍內。
- 原位置放置等高 placeholder，避免頁面高度改變，也讓可見度判定維持在原播放器位置。
- 提供 `enableFloatingVideo` 設定開關，預設啟用。

## 實作方向

- 觀察目標優先使用 `.videoframe`。
- 影片元素優先使用 `.videoframe video`，再退回 `#ani_video_html5_api`、`video.vjs-tech` 與一般 `video`。
- 使用 `IntersectionObserver`，門檻為 0、0.5、0.6、1。
- 使用實際可見高度除以播放器高度，不使用面積比例。
- 固定頂欄透過負值 `rootMargin` 排除：`-60px 0px -8px 0px`。
- 不搬移或重建 `<video>`；只對既有 `.videoframe` 加入或移除浮動 class。
- 使用 Pointer Events 實作四角縮放及整個播放器拖曳，所有位置與尺寸都限制在 viewport 內。
- 拖曳需超過小幅移動門檻才啟動，單純點擊影片仍保留原本播放／暫停行為。
- 返回原位時移除拖曳／縮放控制點，並還原播放器原本的 inline 位置與尺寸。
- 最後手動尺寸只保存在 `initializeFloatingVideo()` 的記憶體狀態，不寫入 GM storage、localStorage 或 sessionStorage。
- 若播放器在頁面載入後才建立，使用 `MutationObserver` 等待第一次出現後再開始觀察。

## 驗收

- 影片未播放時，捲出畫面不浮動。
- 播放中的影片低於 50% 可見時移到右下角，播放不中斷。
- 捲回 50%～60% 時維持浮動；達到 60% 時回到原位。
- 浮動後暫停或播放結束仍留在右下角，捲回至少 60% 才回到原位。
- 進入全螢幕時回到原位。
- 可從四個角落調整浮動播放器寬度與高度，且不超出視窗。
- 手動縮放後捲回原位，再次觸發浮動時沿用相同尺寸；reload 後使用預設尺寸。
- 可從影片畫面或非控制區拖曳播放器，四邊都不能移出瀏覽器可視範圍。
- 點擊原有按鈕、輸入欄位或 Video.js 控制列時不會移動播放器。
- 浮動期間控制列與彈幕正常，頁面原位置不塌陷。
- 關閉設定後，重新載入頁面不啟用浮動功能。
- `npm run check` 通過並產生 `dist/anigamer_kits.user.js`。
